<blockquote class="w3-card-2 w3-padding-medium w3-margin-bottom">
	<%= simple_form_for [fabmoment, comment] do |f| %>
		<!-- Input -->
		<%= f.input_field :text, rows: 4 %>
		<%= f.label :text %>
		<!-- Zenden -->
	  <div class="w3-center w3-margin">
	    <%= f.button :button, class: "w3-btn w3-pale-yellow w3-center" %>
	  </div>
	<% end %>
</blockquote>
